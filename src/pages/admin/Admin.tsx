import { TopBar } from '.'
import { Container } from '../../components'

export const Admin = () => {
  return (
    <Container>
      <h1>Painel do Admin</h1>
      <TopBar />
    </Container>
  )
}
